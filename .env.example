# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom OpenAI Base URL (for proxy or alternative endpoints)
OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Specify the OpenAI model to use for summarization
# See available models at https://platform.openai.com/docs/models
OPENAI_MODEL=gpt-4o-mini

# Optional: Specify the OpenAI model to use for transcript optimization
OPENAI_OPTIMIZATION_MODEL=gpt-3.5-turbo

# Server Configuration (Optional)
HOST=0.0.0.0
PORT=8893

# Whisper Model Configuration (Optional)
# Options: tiny, base, small, medium, large
WHISPER_MODEL_SIZE=base
